
    <table class="table table-light table-md">
        <thead class= "thead-dark">
            <tr>
              <th width="200px">ジャンル検索</th>
            </tr>
            </thead>
        <tbody>
            <% @genres.each do |genre| %>
            <tr width="200px">
             <td><%= link_to genre.name, public_items_path(genre_id: genre.id) %></td>
            </tr>
            <% end %>
        </tbody>
    </table>
